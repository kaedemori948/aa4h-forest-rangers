# Agents（コマンド）

## スライド設定
- data-act: "Claude for AI Agent"
- セクションラベル: 06 — Claude for AI Agent② 利用可能なコマンド

## レイアウト
上から順に:
1. 赤いバー（h-1.5 bg-[#E60012]）
2. コンテンツエリア（flex-1 bg-white px-16 py-10 flex flex-col）

## コンテンツ

### セクションラベル
- スタイル: `text-xs font-semibold tracking-widest text-[#E60012] uppercase mb-4`
- テキスト: 06 — Claude for AI Agent② 利用可能なコマンド

### 2カラム構成（flex gap-14 flex-1 items-center）

#### 左カラム（w-64 shrink-0）
- 見出し（text-3xl font-bold text-gray-900 leading-snug）: コマンドと<br>処理フロー
- 区切り線（w-10 h-0.5 bg-[#E60012] my-5）
- 説明文（text-sm text-gray-500 leading-loose）: 実行可能なSlashコマンドと、コマンド実行後に発生する一連の処理

#### 右カラム（flex-1 flex flex-col items-center justify-center）
- プレースホルダーボックス（`w-full max-w-lg bg-gray-50 border-2 border-dashed border-gray-300 rounded-2xl p-12`）
  - アイコン: 🔧（text-5xl text-gray-300）
  - ステータス（text-xl font-bold text-gray-400）: 検討中
  - 説明（text-sm text-gray-400 text-center leading-relaxed）:
    利用可能なコマンドと実行後に発生する一連の処理については現在検討中です。
