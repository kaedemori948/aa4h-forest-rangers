# UXテスト設計レポート
## AI Assets for HMAX — アセット検索性ユーザーテスト

作成日: 2026-06-22  
最終更新: 2026-06-22（絞り込み機能拡張に伴いテストケースを追加改訂）

---

## 0. 改訂メモ：agents.json と test.json の差分

テストケースは当初 agents.json（デモデータ）をもとに設計していたが、実際の API レスポンス構造は test.json であることが判明した。両者には以下の重大な差分がある。

| フィールド | agents.json（デモ） | test.json（実構造） | テストへの影響 |
|-----------|-------------------|-------------------|--------------|
| `department`（担当部門） | あり | **なし** | TC-04 を廃止 |
| `difficulty`（実装難度） | あり | **なし** | TC-06 を廃止 |
| `certified`（認定済み） | あり | **なし** | TC-03 を改訂 |
| `api`（API対応） | あり | **なし** | 関連テスト削除 |
| `pick`（ピック） | あり | **なし** | 関連テスト削除 |
| `rating` / `ratingCount` | あり | **なし** | ― |
| `capabilities[]` | なし | **あり**（9種） | 新TC追加 |
| `stats.download_count` | なし | **あり** | 新TC追加 |
| `owners[].ownership_type` | なし | **あり** (Director/Lead/Manager) | ― |
| `type.title`（カテゴリ） | カテゴリ一覧を別途定義 | 各アセットの`type`に内包 | 構造は同等 |

現行 agents.html の UI にある「実装難度」「担当部門」「認定済み」「API対応」「ピック」のフィルタはすべて実データに存在しないため、UI を test.json に合わせて更新した後のテストを想定して本レポートを改訂する。

---

## 1. テストの目的と成功指標

**測定したいこと**  
数千件規模に膨らむアセットカタログにおいて、ユーザーが目的のアセットを **3ステップ以内** に発見できるか。

**成功の定義**

| 指標 | 目標値 |
|------|--------|
| タスク完了率 | 80%以上 |
| 3ステップ以内達成率 | 70%以上 |
| タスク完了時間（中央値） | 60秒以内 |
| ユーザー主観評価（SEQ） | 5点以上 / 7点満点 |

> **ステップのカウント方法**: 「ページ遷移・フィルタ操作・検索実行・カード選択」のいずれかを1ステップとして計上する。初期ページ表示はカウントしない。

---

## 2. 実データに基づくサイト構造の整理

### test.json から導出できる検索・絞り込み軸（現行UI実装済み）

| 軸 | フィールド | 値の例 | UIでの実装 | 実装状況 |
|----|---------|------|------------|--------|
| カテゴリ | `type.title` | 資料・文書作成、開発・コード支援 など9種 | サイドバーチップ（**複数選択対応**） | ✅ 済 |
| 機能タグ | `capabilities[].title` | コード支援、データ分析 など9種 | サイドバーピル（**複数選択対応**） | ✅ 済 |
| タグ | `tags[]` | Excel対応、API連携、CRM連携 など10種 | サイドバーピル（**複数選択対応**） | ✅ 済 |
| 更新日 | `last_updated_at` | 2025-10〜2026-06 | 更新日ドロップダウン（過去1ヶ月 / 3ヶ月 / 今年） | ✅ 済 |
| DL閾値 | `stats.download_count` | 0〜332 | DL実績ドロップダウン（10以上 / 50以上 / 100以上） | ✅ 済 |
| 並び順（人気） | `stats.view_count` | 0〜1198 | ソートドロップダウン | ✅ 済 |
| 並び順（利用実績） | `stats.download_count` | 0〜332 | ソートドロップダウン | ✅ 済 |
| 並び順（評価） | `stats.like_count` | 0〜118 | ソートドロップダウン | ✅ 済 |
| キーワード | `title` / `overview`（HTML除去済み） / `owners[].full_name` | 全文 | 検索ボックス | ✅ 済 |

> 全フィルターは組み合わせ使用可能（AND条件）。フィルター適用時はサイドバー上部に「リセット」ボタンが出現。

### 実データには存在しないため、UIから削除される予定の軸

- 実装難度（difficulty）
- 担当部門（department）
- 認定済みバッジ（certified）
- API対応フラグ（api）
- プロデューサーズピック（pick）

---

## 3. テストケース一覧（改訂版）

### ケース設計の方針

- **業務シーン起点の指示文**を使う：「〇〇というアセットを探せ」ではなく「〇〇したいときに使えるものを見つけてほしい」
- 実データの検索・絞り込み軸（カテゴリ / タグ / 並び順 / 更新日 / DL実績 / キーワード）のみを使うケースに限定する
- **各TCの正解アセットは必ず1件**に絞れる条件を設計する（観察者が「完了/未完了」を明確に判定できるようにするため）

---

### TC-01: 実際によく使われているものを選びたい（DL実績フィルタ型）

**タスク指示文**  
> 「社内で情報収集や市場調査に使われているアセットの中で、最も多くの人にダウンロードされているものを1つ見つけてください。」

**正解アセット**: **トレンドウォッチAI**（ID:8046、DL:292）  
> カテゴリ「情報収集・調査」内でダウンロード数1位。同カテゴリを選択し並び順を「利用実績順」にすると最上位に表示される。正解はこの1件のみ。

**実データ上の正解**: `type.title = "情報収集・調査"` かつ `stats.download_count` が最大のアセット（ID:8046）  
**想定パス A（ソート）**: カテゴリ「情報収集・調査」選択 → 並び順「利用実績順（DL）」選択 → 1位のカード選択（3ステップ）  
**想定パス B（フィルタ）**: カテゴリ「情報収集・調査」選択 → 「DL実績 100件以上」選択 → 残り件数を確認して最上位選択（3ステップ）  
**難易度**: 中  
**測定ポイント**:
- 「DL実績フィルタ（閾値）」と「ダウンロード数ソート」のどちらに先に気づくか
- 「人気順（ビュー）」と「利用実績順（DL）」の違いをユーザーが区別できるか
- DL閾値フィルタで結果が絞られすぎた場合にリセットできるか

---

### TC-02: 新しく追加されたものの中から実績で選びたい（更新日×DL複合型）

**タスク指示文**  
> 「先月以降に新しく追加されたテスト・品質保証系のアセットで、最も多くの人に使われているものを1つ教えてください。」

**正解アセット**: **QAテストケース集**（ID:7012、DL:156）  
> カテゴリ「テスト・品質保証」かつ更新日「過去1ヶ月」以内の件数はちょうど3件。その中でダウンロード数が最も多い。正解はこの1件のみ。

**実データ上の正解**: `type.title = "テスト・品質保証"` かつ `last_updated_at` が過去1ヶ月以内 かつ `stats.download_count` が最大のアセット（ID:7012）  
**想定パス**: カテゴリ「テスト・品質保証」選択 → 更新日「過去1ヶ月」選択 → 並び順「利用実績順」選択 → 1位のカード選択（4ステップ）  
**難易度**: 中〜高  
**測定ポイント**:
- 「更新日フィルタ」と「並び順（新着順）」の違い（件数を絞るのか、順番を変えるのか）をユーザーが理解できるか
- 複数フィルターの同時使用（AND条件）が直感的に理解できるか
- 「リセット」ボタンに気づき、フィルターをリセットできるか

---

### TC-03: 名前はうろ覚えで概要だけ覚えている（あいまい検索型・弱点炙り出し）

**タスク指示文**  
> 「以前同僚に教えてもらったアセットで、『自然言語でデータに問い合わせができる』と言っていたものを探してください。名前は覚えていません。」

**正解アセット**: **分析テンプレートセット**（ID:7004）  
> 概要に「自然言語でデータに問い合わせができ、グラフ・サマリーを自動生成します」と明記。「データ分析・診断」カテゴリ内で「自然言語」というキーワードを含むアセットはこの1件のみ。正解はこの1件のみ。  
> ※「社内ナレッジ検索AI」（ID:8019）も「自然言語」を含むが、カテゴリは「ナレッジ・業務支援」であり「データに問い合わせ」という表現が一致しないため不正解。

**実データ上の正解**: `overview` に「自然言語でデータに問い合わせ」を含む `id:7004`  
**想定パス**: 検索ボックスに「自然言語」または「自然言語 データ」と入力 → データ分析系のカード選択（2ステップ）  
**難易度**: 中〜高  
**測定ポイント**:
- overview（HTML文字列のタグ除去後）がキーワード検索の対象になっているか（**実装次第でヒット率が変わるバグ炙り出し型TC**）
- 「自然言語」で2件がヒットしたとき、内容を読み比べて正しい方を選べるか
- ヒットしなかった場合にカテゴリ探索や別キーワードへ切り替えられるか

> **UIへの要件フィードバック**: overview の HTML タグを除去したテキストが検索対象に含まれていなければ、概要文中のキーワードはすべてミスヒットになる。実装時に要確認。

---

### TC-04: ツール連携条件で絞ってさらに実績順に選びたい（タグ×DL複合型）

**タスク指示文**  
> 「Excelと連携して使えるデータ分析系のアセットを探したいです。その中で最もよく使われているものを1つ教えてください。」

**正解アセット**: **在庫最適化分析AI**（ID:8065、DL:167）  
> タグ「Excel対応」かつカテゴリ「データ分析・診断」のアセットのうち、ダウンロード数が最多。正解はこの1件のみ。

**実データ上の正解**: `tags[]` に `"Excel対応"` を含み、`type.title = "データ分析・診断"` かつ `stats.download_count` が最大のアセット（ID:8065）  
**想定パス**: タグ「Excel対応」ピル選択 → カテゴリ「データ分析・診断」選択 → 並び順「利用実績順」→ 1位のカード選択（4ステップ）  
**代替パス**: キーワード「Excel」検索 → 「データ分析」カテゴリで絞り込み → DL上位カード選択  
**難易度**: 中〜高  
**測定ポイント**:
- 「タグ」フィルタの存在に気づくか（機能タグとタグの違いが混乱を生まないか）
- タグとカテゴリを組み合わせる操作が直感的にできるか
- 「Excelと連携」という表現からタグ「Excel対応」にたどり着けるか

> **UIへの要件フィードバック**: タグが付与されていないアセットが混在した場合、フィルタ結果に意図しない抜け漏れが生じる。タグ付与の運用ガイドラインが必要。

---

### TC-05: 複数条件を組み合わせて信頼できるものを探したい（複合フィルタ型）

**タスク指示文**  
> 「最近3ヶ月以内に追加されたアセットの中で、CRM連携ができるものとして最もダウンロードされているものを1つ探してください。」

**正解アセット**: **商談準備アシスタント**（ID:8002、DL:332）  
> 更新日「過去3ヶ月」かつタグ「CRM連携」の条件を組み合わせると複数件がヒットするが、その中でダウンロード数が最多。正解はこの1件のみ。

**実データ上の正解**: `last_updated_at` 過去3ヶ月以内 かつ `tags[]` に `"CRM連携"` を含み、`stats.download_count` が最大のアセット（ID:8002）  
**想定パス**: 更新日「過去3ヶ月」選択 → タグ「CRM連携」ピル選択 → 並び順「利用実績順」→ 1位のカード選択（4ステップ）  
**難易度**: 高  
**測定ポイント**:
- 複数フィルターの同時使用（AND条件）が直感的に理解できるか
- 「リセット」ボタンに気づき、一括解除できるか
- フィルター適用中の状態（何が効いているか）をユーザーが把握できているか

> **UIへの要件フィードバック**: 現在アクティブなフィルターが何件適用中かを示すカウンター（例：「2件のフィルター適用中」）があると、複合状態の把握が容易になる。

---

### TC-06: カテゴリをまたいで最も使われているものを探したい（複数カテゴリ×DL型）

**タスク指示文**  
> 「コード支援またはテスト・品質保証に関係するアセットをまとめて見て、その中で最もよく使われているものを1つ教えてください。」

**正解アセット**: **E2Eテスト自動生成**（ID:8004、DL:280）  
> カテゴリ「開発・コード支援」と「テスト・品質保証」を複数選択するとOR結合で31件が表示される。その中でダウンロード数が最多。正解はこの1件のみ。

**実データ上の正解**: `type.title` が `"開発・コード支援"` または `"テスト・品質保証"` のアセットのうち `stats.download_count` が最大（ID:8004）  
**想定パス**: カテゴリ「開発・コード支援」選択 → 「テスト・品質保証」を追加選択 → 並び順「利用実績順」→ 1位のカード選択（4ステップ）  
**難易度**: 高  
**測定ポイント**:
- カテゴリチップが複数選択できることにユーザーが気づくか
- 複数選択後の結果がOR（どちらか）で表示されることを理解できるか
- 「AND（両方に属するもの）」を期待するユーザーが混乱しないか

> **UIへの要件フィードバック**: 複数選択時に「2カテゴリ選択中」のような状態表示があるとORロジックであることが伝わりやすい。

---

### TC-07: オーナー名で絞ってさらに実績順に選びたい（人名検索×DL型）

**タスク指示文**  
> 「Yamamoto さんが作ったコード支援系のアセットで、最もよく使われているものを探してください。」

**正解アセット**: **依存関係可視化ツール**（ID:8058、DL:189）  
> 「Yamamoto」で検索するとオーナー名がヒットし、カテゴリ「開発・コード支援」に絞ると2件に絞られる。その中でダウンロード数が最多。正解はこの1件のみ。

**実データ上の正解**: `owners[].full_name` に "Yamamoto" を含み、`type.title = "開発・コード支援"` かつ `stats.download_count` が最大のアセット（ID:8058）  
**想定パス**: 検索ボックスに「Yamamoto」入力 → カテゴリ「開発・コード支援」選択 → 並び順「利用実績順」→ 1位のカード選択（4ステップ）  
**難易度**: 中〜高  
**測定ポイント**:
- オーナー名（`owners[].full_name`）が検索対象に含まれているかどうか
- ヒットした結果をさらにカテゴリで絞り込む操作が直感的にできるか
- カード上のオーナー情報の視認性（「Yamamoto さんのアセット」と識別できるか）

> **UIへの要件フィードバック**: 現状の検索が title / overview のみを対象としている場合、オーナー名検索は機能しない。`owners` フィールドを検索対象に含めるかどうかは実装上の判断が必要。

---

## 4. テストケースの優先度マトリクス

| TC | タイトル | 発見できる問題の重大度 | 実施コスト | 優先度 |
|----|---------|-------------------|---------|----|
| TC-01 | カテゴリ直行 | 中（カテゴリラベルの問題） | 低 | 高 |
| TC-02 | 類似カテゴリの迷い | 高（情報アーキテクチャの問題） | 低 | 高 |
| TC-03 | capabilityフィルタ | 高（新UI要素の発見性） | 中 | 高 |
| TC-04 | DL実績フィルタ | 中（フィルタ vs ソートの使い分け） | 低 | 中 |
| TC-05 | 更新日フィルタ | 中（フィルタ vs ソートの区別理解） | 低 | 中 |
| TC-06 | あいまい検索 | 高（検索実装の欠陥検出） | 低 | 高 |
| TC-07 | オーナー名検索 | 中（検索対象範囲の問題） | 低 | 中 |
| TC-08 | タグフィルタ | 高（新UI要素の発見性・ラベル理解） | 中 | 高 |
| TC-09 | 複合フィルタ | 高（AND条件の複合操作、リセット） | 中 | 高 |
| TC-10 | 複数カテゴリ選択 | 中（OR/AND認知のズレ検出） | 中 | 中 |

### テスト回あたりの推奨セット

| 目的 | 実施するTC |
|------|----------|
| 最小セット（60分以内） | TC-01 / TC-02 / TC-06 / TC-08 |
| 標準セット（90分以内） | TC-01 / TC-02 / TC-03 / TC-06 / TC-08 / TC-09 |
| フルセット（120分以内） | TC-01〜TC-10 全件 |

---

## 5. テスト実施方法

### 5-A. モデレートあり対面テスト（推奨）

**対象人数**: 5〜8名  
**対象者プロファイル**:

| グループ | 人数 | 特性 |
|---------|------|------|
| ライトユーザー | 3名 | AIツールに不慣れな一般社員 |
| パワーユーザー | 2名 | AIツールを日常的に使うエンジニア・CoEメンバー |
| 管理職 | 2名 | 導入判断者として全体把握の視点を持つ |

**実施手順**

```
1. ウォームアップ (5分)
   - サイトを自由に閲覧してもらい、どんなサイトか説明してもらう
   - 第一印象を記録：「このサイトで何ができると思いますか？」

2. タスク実施 (30〜40分)
   - 優先度「高」の TC-01、TC-02、TC-03、TC-06、TC-08、TC-09 を必須実施
   - TC-04、TC-05、TC-07、TC-10 は時間に応じて追加
   - シンクアラウドプロトコル：「今何を考えているか声に出してください」
   - モデレーターは誘導しない。3分で詰まった場合のみヒントを出す

3. 事後インタビュー (10分)
   - 「どこが一番困りましたか？」
   - 「カテゴリ以外の絞り込み（機能タグ・タグ・更新日・DL実績）は使いましたか？どう感じましたか？」
   - 「もし1つ機能を追加できるとしたら何ですか？」
   - SEQ（Single Ease Question: 1〜7点）を各タスク直後に取得
```

**記録方法**
- 画面録画 + 発話録音（同意取得済みの前提）
- オブザーバーがステップ数・所要時間・詰まりポイントをリアルタイム記録

---

### 5-B. ファーストクリックテスト（事前スクリーニング）

本テスト前の簡易確認として画像2枚で実施する。

1. 「資料作成を助けてくれるアセットを探すには、まず何をクリックしますか？」  
   → カテゴリチップ / 検索ボックス / ナビ の分布を測る

2. 「機能（Document Generation など）でアセットを絞りたいとき、どこを操作しますか？」  
   → capabilities フィルタの発見性を事前確認する

3. 「最近1ヶ月以内のアセットだけ見たいとき、どこを操作しますか？」  
   → 更新日フィルタ vs 新着ソート vs ナビの選択傾向を事前確認する

4. 「Excelと連携できるアセットを探すとき、まず何をしますか？」  
   → タグフィルタ vs キーワード検索の選択傾向を事前確認する

---

## 6. 観察・記録フォーマット

```
参加者ID: ___
タスクID: ___

操作ログ:
  Step 1:
  Step 2:
  Step 3:
  Step 4（あれば）:

完了 / 未完了: ___
タスク完了時間: ___ 秒
SEQ（1〜7）: ___
詰まったポイント:
発話から拾った気づき:
```

---

## 7. 実データ構造から導出した UX リスク

### リスク1: capabilities フィルタの発見性（重大度：高）
カテゴリ（`type.title`）と capabilities（`capabilities[].title`）は別軸の概念だが、ユーザーには区別が難しい。カテゴリチップしか目に入らない場合、capabilities での絞り込みは使われない。  
**TC-03 でこのリスクを直接測定する。**

### リスク2: overview のキーワード検索カバレッジ（重大度：高）
overview フィールドは HTML 形式で格納されている。HTMLタグを除去したテキストが検索対象になっていない場合、ユーザーが概要文の語彙でキーワード検索してもヒットしない。数千件規模では致命的。  
**TC-06 でこのリスクを直接測定する。**

### リスク3: カテゴリとcapabilityの概念的重複（重大度：中）
「データ分析・診断」（カテゴリ）と「Data Analysis」（capability）のように、名称が類似した軸が複数存在する。ユーザーがどちらを使えばいいか混乱する可能性がある。  
**UI上での両者の役割説明が必要かどうかを TC-03 で確認する。**

### リスク4: オーナー名検索の非対応（重大度：中）
現在の検索実装が title / overview のみを対象としている場合、`owners[].full_name` は検索対象外になる。社内システムでは「あの人が作ったやつ」という人名起点の探索行動が頻繁に起きると予想される。  
**TC-07 でこのリスクを直接測定する。**

### リスク5: 大量件数での選択疲れ（重大度：中）
現在は80件のサンプルだが、数千件規模になったとき、カテゴリ単体では依然として数百件がヒットする。デフォルト並び順（人気順）が適切かどうか、また追加の絞り込み手段（capabilities / タグ / DL閾値 / 更新日）が自然に使われるかを TC-01 / TC-04 / TC-09 で確認する。

### リスク6: タグフィルタとcapabilityの概念混同（重大度：中）【新規】
「機能タグ（capabilities）」と「タグ（tags）」の2種類のピルフィルターが縦に並んでいる。どちらも「タグ」的に見えるためユーザーが混乱し、片方しか使わない可能性がある。ラベルの表現やセクション見出しで役割の違いを明確にすべきかを **TC-08 / TC-03** で確認する。

> capabilities = 「何ができるか」（機能の種類）、tags = 「どんなツールと連携できるか / どんな用途か」（横断的な属性）という役割分担だが、ユーザーに伝わらなければUIの改善が必要。

### リスク7: フィルター複合時の状態把握（重大度：中）【新規】
複数のフィルターを組み合わせると、現在どのフィルターが効いているかが「リセットボタンの有無」だけでは把握しにくい。「3件のフィルター適用中」のようなサマリー表示がないと、ユーザーが自分の絞り込み状態を見失いやすい。特に更新日 × DL閾値 × タグの3重複合で問題が顕在化するかを **TC-09** で確認する。

### リスク8: OR条件の複数カテゴリ選択のメンタルモデルのズレ（重大度：低〜中）【新規】
複数カテゴリを選択するとOR結合で結果が表示される（「どちらかのカテゴリに属するもの全件」）。AND結合（「両方のカテゴリにまたがるもの」）を期待するユーザーは混乱する可能性がある。現実のデータではAND結合で絞りすぎることの方が問題になるが、認知の期待ズレは **TC-10** で測定する。

---

## 8. テスト後のアクション基準

| 結果 | アクション |
|------|----------|
| TC-06 でoverview検索がヒットしない | HTML除去テキストへの全文検索を最優先で実装 |
| TC-03 でcapabilityフィルタが発見されない（発見率 < 40%） | capabilities の UI 露出位置・ラベル・説明文を見直す |
| TC-08 でタグフィルタが発見されない（発見率 < 40%） | タグと capabilities のラベル・配置を統合または整理する |
| TC-08 で「このタグでは見つからない」という反応が多い | タグの種類・粒度・命名を見直す（ユーザー語彙に合わせる） |
| TC-09 で複合フィルタのリセット操作に詰まる | フィルター適用状態の可視化（チップ表示やバッジカウンター）を追加する |
| TC-10 でOR条件を誤解する参加者が過半数 | 複数選択時に「X カテゴリ選択中」の表示を追加し、OR結合であることを明示する |
| TC-02 でカテゴリ間の迷いが多発 | カテゴリ定義の見直し、またはcapabilityタグへの統合を検討 |
| TC-04 / TC-05 でフィルタとソートの区別がつかない | 「絞り込み」「並び替え」のセクション見出しと説明を強化する |
| 全体の3ステップ達成率 < 50% | 検索強化（owners / capabilities / tags / overview 全フィールド対象）を緊急対応 |
| 全体の3ステップ達成率 70%以上 | 現行設計で数千件拡張フェーズへ進む |

---

*本レポートは test.json（実API構造、80件サンプル）および現行 agents.html の UI を分析した上で作成。agents.json のデモフィールド（department / difficulty / certified / api / pick）は実データに存在しないため、対応するテストケースを廃止・改訂した。*
